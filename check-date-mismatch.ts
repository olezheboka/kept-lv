import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const promise = await prisma.promise.findFirst({ include: { statusHistory: { orderBy: { changedAt: 'desc' }, take: 1 } } }); console.log('Promise statusUpdatedAt:', promise?.statusUpdatedAt); console.log('Latest History changedAt:', promise?.statusHistory[0]?.changedAt); } main();
